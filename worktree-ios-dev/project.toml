# worktree-ios-dev/project.toml
# Generated by `worktree-ios-dev-tool proj init`.
# Edit as needed. Run `worktree-ios-dev-tool sim pick` to populate simulator.toml.

schema_version = 2

[project]
path = "BedTerm.xcworkspace"
scheme = "BedTerm"
configuration = "Debug"
simulator_prefix = "BedTerm"
bundle_id = "dev.yjmeqt.bedterm.debug"
product_name = "BedTerm Debug"
min_ios_version = "26.0"

[extras]
xcodebuild_flags = []
