//! Placeholder for `BtIosHostsFlowController` (Phase 4 migration).
//!
//! Will replace Swift's `HostsConnectController` by directly owning the
//! Rust hosts-list VC, nav-bar items, and connect orchestration.
//!
//! Deferred until objc2 `UIAlertController` + layout constraint patterns
//! are verified on-device.

// Stub module — no public API yet.
