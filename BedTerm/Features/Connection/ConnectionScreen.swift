import SwiftUI

struct ConnectionScreen: View {
    @Binding var path: NavigationPath

    var body: some View {
        Text("Connection screen — filled in later tasks.")
            .padding()
    }
}
