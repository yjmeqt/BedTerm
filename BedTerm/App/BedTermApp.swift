import SwiftUI

@main
struct BedTermApp: App {
    var body: some Scene {
        WindowGroup {
            Text("BedTerm — Plan 2 wires up the real UI.")
                .padding()
        }
    }
}
