# Xcode
*.xcuserdata
*.xcworkspace/xcuserdata
DerivedData/
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# SwiftPM
.build/
Packages/
# Package.resolved is the lockfile for this app target — commit it for CI reproducibility.

# CocoaPods
Pods/

# worktree-ios-dev — track project.toml (shared config); ignore derivedData and per-machine simulator.toml.
worktree-ios-dev/*
!worktree-ios-dev/project.toml

# Worktrees
.worktrees/

# Superpowers planning artifacts (kept local, not tracked)
docs/superpowers/
.superpowers/
